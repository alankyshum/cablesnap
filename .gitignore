# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
node_modules

# BLD-743 — local LLM-call cache for scripts/curate-exercise-images.ts
.cache/
# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# BLD-2078 — CanvasKit WASM staged into public/ by scripts/skia-setup.js
# (postinstall). It is a build artifact reproduced from node_modules on every
# install; do not commit the 8 MB binary.
public/canvaskit.wasm

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Android release signing (never commit these; CI provides them from secrets)
android/app/release.keystore
android/keystore.properties
release.keystore
keystore.properties

# Playwright
/test-results/
/playwright-report/

# F-Droid repo (APKs and index are generated by CI)
fdroid/repo/*.apk
fdroid/repo/index-v1.jar
fdroid/repo/index-v2.json
fdroid/repo/index.xml
fdroid/repo/icons*
fdroid/keystore.p12

# EAS build artifacts
*.apk
*.aab

# Pixelslop screenshot artifacts (generated by e2e/screenshot-capture.spec.ts)
.pixelslop/screenshots/*.png
.pixelslop/screenshots/**/*.png
.pixelslop/screenshots/**/*.json
.pixelslop/audits/**
.pixelslop/manifest.json
node_modules.bak/
.playwright-profile/

.env.local

# BLD-2161 — Paperclip agent worktrees (git worktrees used for isolated agent
# runs). These are not source artifacts and must never be committed; each run
# creates and tears down its own worktree automatically.
.paperclip/

# Android debug skill local env
.android-env.sh
modules/expo-wearos-bridge/android/build/

# Jest coverage output
coverage/
